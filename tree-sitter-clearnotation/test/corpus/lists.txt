================
Flat unordered list
================

- Alpha
- Beta
- Gamma

---

(document
  (unordered_list
    (unordered_list_item (unordered_list_marker) (inline_content (text)))
    (unordered_list_item (unordered_list_marker) (inline_content (text)))
    (unordered_list_item (unordered_list_marker) (inline_content (text)))))

================
Flat ordered list
================

1. First
2. Second
3. Third

---

(document
  (ordered_list
    (ordered_list_item (ordered_list_marker) (inline_content (text)))
    (ordered_list_item (ordered_list_marker) (inline_content (text)))
    (ordered_list_item (ordered_list_marker) (inline_content (text)))))

================
Nested unordered list
================

- Top item
  - Nested one
  - Nested two
- Another top

---

(document
  (unordered_list
    (unordered_list_item
      (unordered_list_marker)
      (inline_content (text))
      (list_item_body
        (nested_list
          (unordered_list
            (unordered_list_item (unordered_list_marker) (inline_content (text)))
            (unordered_list_item (unordered_list_marker) (inline_content (text)))))))
    (unordered_list_item (unordered_list_marker) (inline_content (text)))))

================
Nested ordered list
================

1. First
  1. Inner one
  2. Inner two
2. Second

---

(document
  (ordered_list
    (ordered_list_item
      (ordered_list_marker)
      (inline_content (text))
      (list_item_body
        (nested_list
          (ordered_list
            (ordered_list_item (ordered_list_marker) (inline_content (text)))
            (ordered_list_item (ordered_list_marker) (inline_content (text)))))))
    (ordered_list_item (ordered_list_marker) (inline_content (text)))))

================
Multi-paragraph list item
================

- First item

  continuation of first
- Second item

---

(document
  (unordered_list
    (unordered_list_item
      (unordered_list_marker)
      (inline_content (text))
      (list_item_body
        (list_item_continuation
          (inline_content (text)))))
    (unordered_list_item (unordered_list_marker) (inline_content (text)))))

================
Multi-paragraph ordered list item
================

1. First item

   continuation of first
2. Second item

---

(document
  (ordered_list
    (ordered_list_item
      (ordered_list_marker)
      (inline_content (text))
      (list_item_body
        (list_item_continuation
          (inline_content (text)))))
    (ordered_list_item (ordered_list_marker) (inline_content (text)))))

================
List followed by heading
================

- Item one
- Item two

# Heading after list

---

(document
  (unordered_list
    (unordered_list_item (unordered_list_marker) (inline_content (text)))
    (unordered_list_item (unordered_list_marker) (inline_content (text))))
  (heading
    (heading_marker)
    (inline_content (text))))

================
List followed by paragraph
================

- Item one
- Item two

Paragraph after list.

---

(document
  (unordered_list
    (unordered_list_item (unordered_list_marker) (inline_content (text)))
    (unordered_list_item (unordered_list_marker) (inline_content (text))))
  (paragraph
    (paragraph_line
      (inline_content (text)))))

================
Ordered list followed by heading
================

1. First
2. Second

# Next section

---

(document
  (ordered_list
    (ordered_list_item (ordered_list_marker) (inline_content (text)))
    (ordered_list_item (ordered_list_marker) (inline_content (text))))
  (heading
    (heading_marker)
    (inline_content (text))))
